name: Build Atlas Pack

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Atlas CLI
        run: |
          curl -fL --retry 5 --retry-delay 2 --retry-all-errors \
            "https://atlas.nathanm.org/download/cli/latest/linux/x64" -o atlas
          sudo install -m 0755 atlas /usr/local/bin/atlas
          atlas --version

      - name: Request GitHub OIDC token for Atlas
        id: oidc
        run: |
          set -euo pipefail
          response="$(
            curl -fsSL \
              -H "Authorization: bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
              "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=atlas-hub"
          )"
          token="$(echo "${response}" | jq -r '.value')"
          if [ -z "${token}" ] || [ "${token}" = "null" ]; then
            echo "Failed to retrieve OIDC token from GitHub."
            exit 1
          fi
          echo "token=${token}" >> "${GITHUB_OUTPUT}"

      - name: Deploy pack
        env:
          ATLAS_HUB_URL: https://atlas.nathanm.org
        run: |
          atlas deploy \
            --channel dev \
            --commit-hash "${GITHUB_SHA}" \
            --oidc-token "${{ steps.oidc.outputs.token }}"
